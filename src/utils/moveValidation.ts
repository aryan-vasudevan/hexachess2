interface MoveProps {
    gameId: string,
    playerColor: string,
    fromTileId: string,
    toTileId: string,
}

export default function isValidMove({gameId, playerColor, fromTileId, toTileId}: MoveProps) {

}
